// import { notification } from 'antd';

// export default function notify( type:IconType, title:string, message:string) {
//   notification[type]:({ placement: 'bottomLeft', message: title, description: message });
// }
